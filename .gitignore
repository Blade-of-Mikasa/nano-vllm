# 字节码 / 优化后 / DLL 文件
__pycache__/
*.py[cod]
*$py.class

# C 扩展
*.so

# 分发 / 打包
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  通常，这些文件由 Python 脚本根据模板生成，
#  供 PyInstaller 在构建可执行文件前注入日期等信息。
*.manifest
*.spec

# 安装程序日志
pip-log.txt
pip-delete-this-directory.txt

# 单元测试 / 覆盖率报告
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# 翻译文件
*.mo
*.pot

# Django 相关文件：
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask 相关文件：
instance/
.webassets-cache

# Scrapy 相关文件：
.scrapy

# Sphinx 文档
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter 笔记本
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   对于需要在多种环境中运行的库或软件包，可以忽略这些文件；
#   否则，应将其纳入版本控制：
# .python-version

# pipenv
#   根据 pypa/pipenv#598，建议将 Pipfile.lock 纳入版本控制。
#   但在协作场景下，如果存在平台特定依赖或不支持跨平台的依赖，
#   pipenv 可能会安装无法运行的依赖，或无法安装所有必需的依赖。
#Pipfile.lock

# UV
#   与 Pipfile.lock 类似，通常建议将 uv.lock 纳入版本控制。
#   为确保可复现性，二进制软件包尤其建议这样做；库项目则更常忽略该文件。
#uv.lock

# poetry
#   与 Pipfile.lock 类似，通常建议将 poetry.lock 纳入版本控制。
#   为确保可复现性，二进制软件包尤其建议这样做；库项目则更常忽略该文件。
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock
#poetry.toml

# pdm
#   与 Pipfile.lock 类似，通常建议将 pdm.lock 纳入版本控制。
#pdm.lock
#   pdm 将项目级配置保存在 .pdm.toml 中，但建议不要将其纳入版本控制。
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582；例如 github.com/David-OConnor/pyflow 和 github.com/pdm-project/pdm 会使用它
__pypackages__/

# Celery 相关文件
celerybeat-schedule
celerybeat.pid

# SageMath 解析生成的文件
*.sage.py

# 环境目录
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder 项目设置
.spyderproject
.spyproject

# Rope 项目设置
.ropeproject

# mkdocs 文档
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre 类型检查器
.pyre/

# pytype 静态类型分析器
.pytype/

# Cython 调试符号
cython_debug/

# PyCharm
#  JetBrains 专用模板维护在单独的 JetBrains.gitignore 文件中，地址为
#  https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore。
#  可以将其添加到全局 gitignore，或合并到此文件中。也可以取消下面一行的注释，
#  以忽略整个 idea 文件夹，但这种彻底的做法并不推荐。
.idea/

# Abstra
# Abstra 是一个由 AI 驱动的流程自动化框架。
# 忽略包含用户凭据、本地状态和设置的目录。
# 详情请参阅 https://abstra.io/docs
.abstra/

# Visual Studio Code
#  Visual Studio Code 专用模板维护在单独的 VisualStudioCode.gitignore 文件中，地址为
#  https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore。
#  可以将其添加到全局 gitignore，或合并到此文件中。也可以取消下面一行的注释，
#  以忽略整个 vscode 文件夹。
.vscode/

# Ruff 相关文件：
.ruff_cache/

# PyPI 配置文件
.pypirc

# Cursor
#  Cursor 是一款由 AI 驱动的代码编辑器。`.cursorignore` 用于指定不参与自动补全、
#  代码分析等 AI 功能的文件或目录。建议用它排除敏感数据，详情请参阅
#  https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore
